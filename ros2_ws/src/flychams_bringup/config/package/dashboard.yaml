/flychams/**:
  ros__parameters:
    # GUI parameters
    gui:
      gui_update_rate: 20.0 # (Hz)
    # Visualization parameters
    visualization:
      metrics_update_rate: 10.0 # (Hz)
      markers_update_rate: 10.0 # (Hz)
